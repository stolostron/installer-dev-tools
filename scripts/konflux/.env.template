# JIRA Configuration
# Copy this file to .env and fill in your actual values
# Then source it before running the script: source .env
# WARNING: Never commit .env file with real credentials!

# JIRA Username (required - usually your email)
# Example: your.email@redhat.com
export JIRA_USER=""

# JIRA API Token (required for jira-cli authentication)
# For Red Hat JIRA, create a Personal Access Token at:
# https://issues.redhat.com/secure/ViewProfile.jspa -> Personal Access Tokens
export JIRA_API_TOKEN=""

# JIRA Authentication Type (required when using Personal Access Token)
# Set to "bearer" for Personal Access Token authentication
export JIRA_AUTH_TYPE="bearer"

# JIRA project key (optional, defaults to "ACM")
# Example: ACM, MCE, OCMUI
export JIRA_PROJECT="ACM"
